<UserControl x:Class="Digital_Indicator.Module.Display.Views.DiameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Digital_Indicator.Module.Display.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--<ItemsControl ItemsSource="{Binding SerialPortList}" />-->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content="Diameter"  FontSize="26" HorizontalAlignment="Left" />
                <Label Content="{Binding Diameter}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" FontSize="26" HorizontalAlignment="Right"  />
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content="Highest Value" FontSize="26" HorizontalAlignment="Left"/>
                <Label Content="{Binding HighestValue}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" FontSize="26" HorizontalAlignment="Right"  />
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content="Lowest Value" FontSize="26" HorizontalAlignment="Left"/>
                <Label Content="{Binding LowestValue}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" FontSize="26" HorizontalAlignment="Right"  />
            </Grid>
        </StackPanel>
        <oxy:PlotView Model="{Binding HistoricalModel}" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="9" Grid.RowSpan="3"/>
        <oxy:PlotView Model="{Binding RealTimeModel}" Grid.Column="0" Grid.Row="6" Grid.ColumnSpan="9" Grid.RowSpan="3"/>

        <Grid Grid.Column="8" Margin="0,0,20,0" Grid.Row="0" Grid.RowSpan="2">
            <StackPanel>
                <Button Content="Start Capture" Command="{Binding StartCapture}" VerticalAlignment="Bottom"  MinHeight="35" Margin="0,0,0,10" />
                <Button Content="Stop Capture" Command="{Binding StopCapture}" VerticalAlignment="Bottom"  MinHeight="35" Margin="0,0,0,45" />
            </StackPanel>
        </Grid>

        <Grid Grid.Column="8" Grid.Row="0" Margin="0,0,20,-30" VerticalAlignment="Bottom" Grid.RowSpan="2">

            <Button Grid.ColumnSpan="2" Content="Reset Graph" Command="{Binding ResetGraph}" MinHeight="35"/>
        </Grid>
    </Grid>
</UserControl>
